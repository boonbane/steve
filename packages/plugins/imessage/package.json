{
  "name": "steve-plugin-imessage",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/daemon"
  ],
  "scripts": {
    "test": "bun test --cwd packages/core && bun test --cwd packages/cli && bun test --cwd packages/daemon",
    "typecheck": "bun run --cwd packages/core typecheck && bun run --cwd packages/cli typecheck && bun run --cwd packages/daemon typecheck",
    "native:build": "bun run --cwd packages/core native:build",
    "native:clean": "bun run --cwd packages/core native:clean",
    "native:smoke": "bun run --cwd packages/core native:smoke",
    "cli": "bun run --cwd packages/cli cli",
    "daemon": "bun run --cwd packages/daemon start"
  }
}
