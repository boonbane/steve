{
  "name": "steve-plugin-imessage",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/chart"
  ],
  "scripts": {
    "test": "bun test --cwd packages/core && bun test --cwd packages/cli",
    "typecheck": "bun run --cwd packages/core typecheck && bun run --cwd packages/cli typecheck && bun run --cwd packages/chart typecheck",
    "native:build": "bun run --cwd packages/core native:build",
    "native:clean": "bun run --cwd packages/core native:clean",
    "native:smoke": "bun run --cwd packages/core native:smoke",
    "cli": "bun run --cwd packages/cli cli"
  }
}
